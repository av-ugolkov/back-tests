version: '3.9'

services:
  example:
    build: .
    ports:
      - '3000:3000'
    extra_hosts:
      - 'host.docker.internal:host-gateway'

  prometheus:
    image: prom/prometheus:v3.2.1
    volumes:
      - /home/alex/DockerShareFiles/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - '9090:9090'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
